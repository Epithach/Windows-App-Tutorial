<Page
    x:Class="UniversalApp.LaudableLayoutChallenge"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UniversalApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="57*"/>
            <ColumnDefinition Width="263*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ACME Sales Corp" VerticalAlignment="Center" FontSize="72" Grid.ColumnSpan="2" Margin="0,12"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Grid.ColumnSpan="2">
            <TextBlock Text="First Name :"/>
            <TextBox></TextBox>
            <TextBlock Text="Last Name :"/>
            <TextBox/>
            <TextBlock Text="Email :"/>
            <TextBox/>
            <Button Content="Save"/>
        </StackPanel>
    </Grid>
</Page>
