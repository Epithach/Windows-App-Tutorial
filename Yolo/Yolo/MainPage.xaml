<Page
    x:Class="Yolo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Yolo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

     <TextBlock Text="Sign In" Margin="10,10,0,0" FontSize="20" FontFamily="Segoe WP Light"></TextBlock>


        <StackPanel Margin="20,50, 0, 0">
          <!--        <TextBlock x:Name="TEXT" Text="{Binding TextYolo}" />
                  <Button Content="Click me!" Command="{Binding OnButtonClick}" /> -->
            <TextBlock Text="First Name :"/>
            <TextBox Width="500" HorizontalAlignment="Left" Text="{Binding FirstName}"></TextBox>
            <TextBlock Text="Last Name :"/>
            <TextBox Width="500" HorizontalAlignment="Left"/>
            <TextBlock Text="Email :"/>
            <TextBox Width="500" HorizontalAlignment="Left"/>
            <Button Content="Save" Margin="450, 10, 0, 0" Command="{Binding OnButtonClick}"/>

            <TextBlock Text="{Binding Info}"/>

        </StackPanel>
    </Grid>
</Page>
